                                                      最近在项目中用RN开发pda软件问题记录
   由于RN框架对于异常捕获不准就会卸载整个ui导致异常崩溃！
   1.空指针- 泛指代码中出现的underfined，null 都有可能出现异常崩溃
   2.try-catch - 只在判断代码层面的错误
   3.loading- 正确应用，如果loading层面没有正常消失导致蒙层一直存在从而导致假死现象
   4.页面销毁- 缓存过多会导致程序崩溃
